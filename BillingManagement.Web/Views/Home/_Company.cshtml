@model BillingManagement.Web.Models.Company
    
@Html.HiddenFor(model => model.Id)

<table class="hovertable">
    <tr>
        <th>Company Name</th>
        <th>Sites</th>
        <th></th>
    </tr>
    <tr>
        <td>
            @Model.Name
        </td>

        <td>
            <table class="hovertable">
                @foreach (var site in Model.SitesList)
                {
                    @Html.Partial("_Site", site)
                }       
            </table>
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "Companies")
        </td>
    </tr>
</table>
